    VARIABILES>
    esto n : numerus 42;
    esto d : decimalis 2.5;
    esto c : littera 'A';
    esto b : bool verum;
    MAIOR>
    >> "entero: " + n;
    >> "flotante: " + d;
    >> "caracter: " + c;
    >> "booleano: " + b;
    FINIS;